import { MarkCompletedDirective } from './mark-completed.directive';

describe('MarkCompletedDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkCompletedDirective();
    expect(directive).toBeTruthy();
  });
});
